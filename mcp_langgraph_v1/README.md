# MCP LangGraph v1

## What this is
A LangGraph-based MCP host application with:
- Analytics MCP server backed by Azure PostgreSQL
- Docs MCP server backed by local JSON docs
- Agent logic embedded inside LangGraph nodes

## Setup

1. Create and activate a Python virtual environment
2. Install dependencies:
   ```bash
   pip install -r requirements.txt